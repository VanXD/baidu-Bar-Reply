USE:
Fill cookies into cookie input.
Fill url that with parameters into parameters input.

Both of its we can get from F12 and network button.

The delay input set the send delay.


Environment:
tomcat 7.0
java 6+

Directed by : VanXD